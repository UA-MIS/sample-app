# PLACEHOLDER SealedSecret for sample-staging (§5.3, D-006). Per-namespace strict
# scope: valid ONLY in namespace sample-staging. Regenerate with kubeseal once
# T4 is up — see .devops/README.md. Decrypts to Secret `sample-secret`.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: sample-secret
  namespace: sample-staging
spec:
  encryptedData:
    app-secret: AgABCDEF_PLACEHOLDER_REPLACE_WITH_KUBESEAL_staging_xxxxxxxx==
  template:
    metadata:
      name: sample-secret
    type: Opaque
