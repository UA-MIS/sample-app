# PLACEHOLDER SealedSecret for sample-dev (§5.3, D-006). Per-namespace strict
# scope: this seal is valid ONLY in namespace sample-dev. Regenerate with
# kubeseal against the live controller once T4 is up — see .devops/README.md.
# Decrypts to Secret `sample-secret` (key app-secret) → Deployment APP_SECRET.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: sample-secret
  namespace: sample-dev
spec:
  encryptedData:
    app-secret: AgABCDEF_PLACEHOLDER_REPLACE_WITH_KUBESEAL_dev_xxxxxxxxxxxx==
  template:
    metadata:
      name: sample-secret
    type: Opaque
