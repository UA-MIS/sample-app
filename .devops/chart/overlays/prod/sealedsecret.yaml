# PLACEHOLDER SealedSecret for sample-prod (§5.3, D-006). Per-namespace strict
# scope: valid ONLY in namespace sample-prod. Regenerate with kubeseal once T4 is
# up — see .devops/README.md. Decrypts to Secret `sample-secret`.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: sample-secret
  namespace: sample-prod
spec:
  encryptedData:
    app-secret: AgABCDEF_PLACEHOLDER_REPLACE_WITH_KUBESEAL_prod_xxxxxxxxxx==
  template:
    metadata:
      name: sample-secret
    type: Opaque
